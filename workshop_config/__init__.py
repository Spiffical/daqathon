"""Participant-editable workshop configuration values.

The notebooks and helper scripts read their starting dataset presets, label
meanings, and shared defaults from this package. Code that *uses* those values
still lives under ``scripts/``.
"""

